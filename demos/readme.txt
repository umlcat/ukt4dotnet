demos
=====

This folder does not store software packages, software libraries, controls,
inclusion files, and similars.

Instead, this folder stores small programs that serve as test for those
libraries.

It is recommended that these programs should be kept as small,
as posible, trying, to use the less possible dependencies.